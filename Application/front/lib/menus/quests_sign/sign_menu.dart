import 'package:flutter/material.dart';

/// Menu opened when interacting with the quests sign.
class SignMenu extends StatelessWidget {
  const SignMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
